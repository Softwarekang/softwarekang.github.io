<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://softwarekang.github.io</id>
    <title>kang note site</title>
    <updated>2022-07-05T09:18:10.615Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://softwarekang.github.io"/>
    <link rel="self" href="https://softwarekang.github.io/atom.xml"/>
    <subtitle>愿以年少济沧海</subtitle>
    <logo>https://softwarekang.github.io/images/avatar.png</logo>
    <icon>https://softwarekang.github.io/favicon.ico</icon>
    <rights>All rights reserved 2022, kang note site</rights>
    <entry>
        <title type="html"><![CDATA[Eventhub  Details]]></title>
        <id>https://softwarekang.github.io/post/eventhub-details/</id>
        <link href="https://softwarekang.github.io/post/eventhub-details/">
        </link>
        <updated>2022-06-02T02:42:49.000Z</updated>
        <summary type="html"><![CDATA[<h3 id="介绍">介绍</h3>
<p>Eventhub 是一种无服务器事件总线，可使用从您的应用程序生成的事件，更轻松地大规模构建事件驱动型应用程序。EventBridge 提供从事件源（例如 TAPD ）到目标对象（例如 Faas、SaaS ）的实时数据流。您可以设置路由规则，以确定将数据发送到何处，从而构建在事件发布者和使用者完全解耦的情况下对数据源进行实时响应的应用程序架构。</p>
]]></summary>
        <content type="html"><![CDATA[<h3 id="介绍">介绍</h3>
<p>Eventhub 是一种无服务器事件总线，可使用从您的应用程序生成的事件，更轻松地大规模构建事件驱动型应用程序。EventBridge 提供从事件源（例如 TAPD ）到目标对象（例如 Faas、SaaS ）的实时数据流。您可以设置路由规则，以确定将数据发送到何处，从而构建在事件发布者和使用者完全解耦的情况下对数据源进行实时响应的应用程序架构。</p>
<!-- more -->
<h3 id="优势">优势</h3>
<h4 id="构建事件驱动型架构">构建事件驱动型架构</h4>
<p>EventHub 简化了构建事件驱动型架构的流程。借助 EventHub，您的事件目标不需要知道事件源，因为您可以直接进行筛选并发布到  EventHub。无需任何设置。通过松散耦合的事件驱动型架构提高开发人员敏捷性以及应用程序弹性。</p>
<h4 id="消息引擎能力">消息引擎能力</h4>
<ul>
<li>Eventhub 具备事件源、事件规则、事件目标等概念。同时具备常见消息引擎（kafka、rocketmq)能力，在 push/sub 场景下同样适用。</li>
<li>提供 Go SDK 支持生产、消费事件、延时事件、死信队列、消息重放、tag 过滤、request/reply 能力。</li>
</ul>
<h4 id="快速连接-saas-应用">快速连接 SaaS 应用</h4>
<p>EventHub 从受支持的 SaaS 应用程序中提取数据，并将其发送到 SaaS 目标（通过 API 目标 – 事件的一种 HTTP 调用终端节点目标），而且不需要编写自定义集成代码。您可以使用  EventHub 连接您的 SaaS 应用程序，或使用 SaaS 应用程序中的事件来触发相应业务的工作流程。</p>
<h4 id="安全性-可观测与可治理能力">安全性 &amp; 可观测与可治理能力</h4>
<ul>
<li>支持HTTP/HTTPS协议。</li>
<li>提供事件的可观测性能力，支持事件查询、审计以及全链路的追踪；</li>
<li>提供事件的可治理能力，支持事件流控、事件回放、以及事件重试策略等；</li>
</ul>
<h4 id="高性能高可用">高性能&amp;高可用</h4>
<ul>
<li>服务可用性 99.95%，Region 化、多可用区、分布式集群化部署，具备极强的容灾能力。</li>
</ul>
<h3 id="架构设计">架构设计</h3>
<figure data-type="image" tabindex="1"><img src="https://softwarekang.github.io/post-images/1655187277632.png" alt="" loading="lazy"></figure>
<h4 id="事件源">事件源：</h4>
<ul>
<li>事件的来源，负责生产事件。</li>
</ul>
<h4 id="事件总线">事件总线：</h4>
<ul>
<li>事件的中转站，负责事件的中间转储。</li>
</ul>
<h4 id="事件规则">事件规则：</h4>
<ul>
<li>用于监控特定类型的事件。当发生匹配事件时，事件会被路由到与事件规则关联的事件目标。</li>
</ul>
<h4 id="事件目标">事件目标：</h4>
<ul>
<li>事件的处理终端，负责消费事件。</li>
</ul>
<h4 id="schema">Schema：</h4>
<ul>
<li>定义事件的结构。针对发送到自定义事件总线的事件，您可以自定义 Schema，也可以通过过滤事件，推导出事件结构从而得到 Schema。以上 Schema 都存储在 Schema 注册表中，事件目标可以从 Schema 注册表中下载事件 Schema 的 Code Binding，实现快速访问事件。</li>
</ul>
<h3 id="业务场景">业务场景</h3>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[序列化 - Protobuf 原理]]></title>
        <id>https://softwarekang.github.io/post/xu-lie-hua-protobuf-yu-json-dui-bi/</id>
        <link href="https://softwarekang.github.io/post/xu-lie-hua-protobuf-yu-json-dui-bi/">
        </link>
        <updated>2022-04-16T07:32:27.000Z</updated>
        <summary type="html"><![CDATA[<p>protobuf 简介<br>
protobuf 即 Protocol Buffers，是一种轻便高效的结构化数据存储格式，与语言、平台无关，可扩展可序列化。protobuf 性能和效率大幅度优于 JSON、XML 等其他的结构化数据格式。protobuf 是以二进制方式存储的，占用空间小，但也带来了可读性差的缺点。</p>
]]></summary>
        <content type="html"><![CDATA[<p>protobuf 简介<br>
protobuf 即 Protocol Buffers，是一种轻便高效的结构化数据存储格式，与语言、平台无关，可扩展可序列化。protobuf 性能和效率大幅度优于 JSON、XML 等其他的结构化数据格式。protobuf 是以二进制方式存储的，占用空间小，但也带来了可读性差的缺点。</p>
<!-- more -->
<p>protobuf 在通信协议和数据存储等领域应用广泛，主流开源 RPC 框架 Dubbo、GRPC 等均内置了 protobuf 序列化协议，在高流量冲击下 protobuf 性能表现优异，本文来一起探索 protobuf 实现原理。</p>
<h2 id="protobuf-安装">protobuf 安装</h2>
<h3 id="protoc-工具安装">protoc 工具安装</h3>
<p>从官网<a href="https://github.com/protocolbuffers/protobuf/releases">protobuf releases</a>中选择自己机器对应环境的版本，下载对应版本解压并移动到自己机器环境的bin目录。</p>
<pre><code class="language-bash"># 下载安装包
$ wget https://github.com/protocolbuffers/protobuf/releases/download/v3.11.2/protoc-3.11.2-linux-x86_64.zip
# 解压到 /usr/local 目录下
$ sudo 7z x protoc-3.11.2-linux-x86_64.zip -o/usr/local
</code></pre>
<h3 id="protoc-gen-go-安装">protoc-gen-go 安装</h3>
<p>GoLang 中使用 protobuf，需要安装 protoc-gen-go，此工具将 proto 文件转化为 Go 文件。</p>
<pre><code class="language-bash">go get -u github.com/golang/protobuf/protoc-gen-go
</code></pre>
<p>protoc-gen-go 将自动安装到 $GOPATH/bin 目录下，也需要将这个目录加入到环境变量中。</p>
<h2 id="protobuf-使用">protobuf 使用</h2>
<h3 id="定义-message-消息体">定义 Message 消息体</h3>
<p>新人首先阅读 protobuf 语法教程<a href="https://developers.google.com/protocol-buffers/docs/proto3">protobuf3语法</a>，<a href="https://developers.google.com/protocol-buffers/docs/proto">protobuf2语法</a>，首先定义一个简单的消息体案例。</p>
<pre><code class="language-proto">// bin bash: protoc --go_out=. *.proto
syntax = &quot;proto3&quot;;
option go_package = &quot;/pb&quot;; // 可选参数，生成的 Go 代码在 ./pb 目录下。

// object for student
message Student {
  string name = 1;
  bool male = 2;
  repeated int32 scores = 3;
}
</code></pre>
<p>当前目录下执行生成 Go 代码命令</p>
<pre><code class="language-sh">protoc --go_out=. *.proto
</code></pre>
<p><img src="https://softwarekang.github.io/post-images/1650165694286.png" alt="" loading="lazy"><br>
可以看到在 pb 目录下生成了名为 student.pb.go 的文件。<a href="https://github.com/Softwarekang/expamle/blob/54bdc8419ceee9280a8ea6345c58e41316f49a8f/codec/serialization/pb/student.pb.go#L31">student.pb.go文件内容</a>，文件主要提供了 student 结构体在序列化实现时的一些方法。</p>
<h3 id="简单使用">简单使用</h3>
<pre><code class="language-go">package main

import (
	&quot;encoding/json&quot;
	&quot;log&quot;

	&quot;github.com/golang/protobuf/proto&quot;

	&quot;github.com/Softwarekang/expamle/codec/serialization/pb&quot;
)

func protobufCodec(student *pb.Student) {
	bytes, err := proto.Marshal(student)
	if err != nil {
		log.Fatalf(&quot;marshal err:%v&quot;, err)
	}

	var target pb.Student
	if err := proto.Unmarshal(bytes, &amp;target); err != nil {
		log.Fatalf(&quot;unmarshal err:%v&quot;, err)
	}
}
</code></pre>
<p>如上代码，简单的概括描述了，使用 proto 完成 student 结构体 Marshal和UnMarshal 的用法，与常规 Json 库使用无差别。</p>
<h2 id="性能对比">性能对比</h2>
<p>基于 Go 内置的基准测试（benchmark）能力进行 protobuf 与主流 json 序列化方式性能压测。</p>
<h3 id="压测环境">压测环境</h3>
<table>
<thead>
<tr>
<th>系统类型</th>
<th>CPU</th>
<th>内存</th>
</tr>
</thead>
<tbody>
<tr>
<td>linux</td>
<td>1</td>
<td>512G</td>
</tr>
</tbody>
</table>
<h3 id="压测代码">压测代码</h3>
<p><a href="https://github.com/Softwarekang/expamle/blob/54bdc8419ceee9280a8ea6345c58e41316f49a8f/codec/serialization/expamle.go">源码地址</a><br>
<a href="https://github.com/Softwarekang/expamle/blob/8f138b8a6b113b021c848b8fb7a4a6bdbfc4d856/codec/serialization/README.md">压测步骤</a></p>
<pre><code class="language-go">func jsonCodec(student *pb.Student) {
	bytes, err := json.Marshal(student)
	if err != nil {
		log.Fatalf(&quot;marshal err:%v&quot;, err)
	}

	var target pb.Student
	if err := json.Unmarshal(bytes, &amp;target); err != nil {
		log.Fatalf(&quot;unmarshal err:%v&quot;, err)
	}
}

func protobufCodec(student *pb.Student) {
	bytes, err := proto.Marshal(student)
	if err != nil {
		log.Fatalf(&quot;marshal err:%v&quot;, err)
	}

	var target pb.Student
	if err := proto.Unmarshal(bytes, &amp;target); err != nil {
		log.Fatalf(&quot;unmarshal err:%v&quot;, err)
	}
}
</code></pre>
<h3 id="性能压测结果">性能压测结果</h3>
<h4 id="耗时压测结果">耗时压测结果</h4>
<table>
<thead>
<tr>
<th>次数</th>
<th>protobuf</th>
<th>json</th>
</tr>
</thead>
<tbody>
<tr>
<td>10</td>
<td>0.107s</td>
<td>0.109s</td>
</tr>
<tr>
<td>100</td>
<td>0.109s</td>
<td>0.112s</td>
</tr>
<tr>
<td>1000</td>
<td>0.107s</td>
<td>0.117s</td>
</tr>
<tr>
<td>10000</td>
<td>0.113s</td>
<td>0.126s</td>
</tr>
<tr>
<td>100000</td>
<td>0.148s</td>
<td>0.281s</td>
</tr>
<tr>
<td>1000000</td>
<td>0.492s</td>
<td>1.817s</td>
</tr>
<tr>
<td>10000000</td>
<td>3.922s</td>
<td>17.273s</td>
</tr>
</tbody>
</table>
<h4 id="内存压测结果">内存压测结果</h4>
<table>
<thead>
<tr>
<th>次数</th>
<th>protobuf</th>
<th>json</th>
</tr>
</thead>
<tbody>
<tr>
<td>10</td>
<td>155B</td>
<td>638B</td>
</tr>
<tr>
<td>100</td>
<td>145B</td>
<td>466B</td>
</tr>
<tr>
<td>1000</td>
<td>144B</td>
<td>449B</td>
</tr>
<tr>
<td>10000</td>
<td>144B</td>
<td>448B</td>
</tr>
<tr>
<td>100000</td>
<td>144B</td>
<td>448B</td>
</tr>
<tr>
<td>1000000</td>
<td>144B</td>
<td>448B</td>
</tr>
<tr>
<td>10000000</td>
<td>144B</td>
<td>448B</td>
</tr>
</tbody>
</table>
<p>从压测结果来看两种序列化方式，内存方面 protobuf 相比 json 内存使用量少 2/3，耗时方面次数在10000以下时两者差别不大，但次数为 10w、100w、1000w 时，protobuf 比 json 快 3-4倍，可以看出 protobuf 在时间和内存方面都优胜于 json。</p>
<h2 id="protobuf-原理实现">protobuf 原理实现</h2>
<h3 id="tlv-数据存储结构">TLV 数据存储结构</h3>
<p>TLV 即 Tag-length(可选)-value，分别从字段标示、长度、值来描述一个具体的数据，所有的数据组合起来就是一个二进制数据流。<br>
<img src="https://softwarekang.github.io/post-images/1650171175876.png" alt="" loading="lazy"></p>
<h3 id="protobuf-编码和存储方式">protobuf 编码和存储方式</h3>
<p><img src="https://softwarekang.github.io/post-images/1650171315662.png" alt="" loading="lazy"><br>
上图主要展示 protobuf 针对不同的数据采用的编码方式，protobuf 在内存优化和速度优胜更多取决于编码和存储设计，后面我们会针对每中编码实现做探索。</p>
<h4 id="varint-编码">Varint 编码</h4>
<p>Varint是一种使用一个或多个字节序列化整数的方法，会把整数编码为变长字节。对于32位整型数据经过Varint编码后需要1到5个字节，小的数字使用1个byte，大的数字使用5个bytes。64位整型数据编码后占用1~10个字节。在实际场景中小数字的使用率远远多于大数字，因此通过Varint编码对于大部分场景都可以起到很好的压缩效果。<br>
varint 核心原理为减少数字的字节数来减少内存使用<br>
<img src="https://softwarekang.github.io/post-images/1650176366469.png" alt="" loading="lazy"><br>
varint 编码实现: <a href="https://github.com/Softwarekang/expamle/blob/b250c25b55d639338c8f55820c8951174b6407f8/codec/serialization/varint/varint.go">varint源码</a></p>
<pre><code class="language-go">package varint

const maxVarintBytes = 10 // maximum length of a varint

// 返回Varint类型编码后的字节流
func EncodeVarint(x uint64) []byte {
	var buf [maxVarintBytes]byte
	var n int
	// 下面的编码规则需要详细理解:
	// 1.每个字节的最高位是保留位, 如果是1说明后面的字节还是属于当前数据的,如果是0,那么这是当前数据的最后一个字节数据
	//  看下面代码,因为一个字节最高位是保留位,那么这个字节中只有下面7bits可以保存数据
	//  所以,如果x&gt;127,那么说明这个数据还需大于一个字节保存,所以当前字节最高位是1,看下面的buf[n] = 0x80 | ...
	//  0x80说明将这个字节最高位置为1, 后面的x&amp;0x7F是取得x的低7位数据, 那么0x80 | uint8(x&amp;0x7F)整体的意思就是
	//  这个字节最高位是1表示这不是最后一个字节,后面7为是正式数据! 注意操作下一个字节之前需要将x&gt;&gt;=7
	// 2.看如果x&lt;=127那么说明x现在使用7bits可以表示了,那么最高位没有必要是1,直接是0就ok!所以最后直接是buf[n] = uint8(x)
	//
	// 如果数据大于一个字节(127是一个字节最大数据), 那么继续, 即: 需要在最高位加上1
	for n = 0; x &gt; 127; n++ {
		// x&amp;0x7F表示取出下7bit数据, 0x80表示在最高位加上1
		buf[n] = 0x80 | uint8(x&amp;0x7F)
		// 右移7位, 继续后面的数据处理
		x &gt;&gt;= 7
	}
	// 最后一个字节数据
	buf[n] = uint8(x)
	n++
	return buf[0:n]
}
</code></pre>
<h3 id="资料文献">资料文献</h3>
<p><a href="https://geektutu.com/post/hpg-benchmark.html">GoLang BenchMark</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Istio-proxy相关概念]]></title>
        <id>https://softwarekang.github.io/post/istio-proxy-xiang-guan-gai-nian/</id>
        <link href="https://softwarekang.github.io/post/istio-proxy-xiang-guan-gai-nian/">
        </link>
        <updated>2020-02-04T12:39:47.000Z</updated>
        <summary type="html"><![CDATA[<h3 id="istio-proxy">istio-proxy</h3>
<p>Istio代理是可在客户端和服务器端使用的微服务代理，并形成微服务网格。代理支持大量功能。</p>
]]></summary>
        <content type="html"><![CDATA[<h3 id="istio-proxy">istio-proxy</h3>
<p>Istio代理是可在客户端和服务器端使用的微服务代理，并形成微服务网格。代理支持大量功能。</p>
<!-- more -->
<h5 id="客户端功能">客户端功能：</h5>
<ul>
<li>发现和负载平衡。代理可以使用几个标准的服务发现和负载平衡API，以有效地将流量分配给服务。</li>
<li>凭证注入。代理可以通过连接隧道或特定于协议的机制（例如HTTP请求的JWT令牌）注入客户端身份。</li>
<li>连接管理。代理管理与服务的连接，处理运行状况检查，重试，故障转移和流控制。</li>
<li>监控和记录。代理可以报告客户端指标并记录到混合器。</li>
</ul>
<h5 id="服务器端功能">服务器端功能：</h5>
<ul>
<li>速率限制和流量控制。代理可以防止后端系统过载，并提供客户端感知的速率限制。</li>
<li>协议翻译。代理是gRPC网关，提供JSON-REST和gRPC之间的转换。</li>
<li>认证与授权。代理支持多种身份验证机制，并且可以使用客户端身份通过混合器执行授权检查。</li>
<li>监控和记录。代理可以报告服务器端指标并记录到混合器。</li>
</ul>
<h3 id="istio-proxy和envoy的关系">istio proxy和Envoy的关系</h3>
<p>istio proxy这个项目工程既包含引用了Envoy的源码，还在此基础上自己做了扩展，这个扩展是通过Envoy filter（过滤器）的形式来提供，这样的话就可以使得proxy代理将策略执行决策委托给Mixer，这样就解释了为什么Mixer可以被设计为提供策略和遥测的组件，Mixer-&gt;istio proxy-&gt;Envoy这种形式来控制。这样通过这个方式就能：</p>
<ul>
<li>使用到Envoy的全部功能</li>
<li>基于Envoy做扩展，结合istio本身做处理</li>
</ul>
<h4 id="proxy-项目编译">proxy 项目编译</h4>
<h6 id="环境">环境</h6>
<p>首先参考Bazel的官方文档安装Bazel，并且需要安装gcc等相关工具。</p>
<pre><code class="language-shell">git clone https://github.com/istio/proxy.git
cd proxy
make build_envoy
</code></pre>
<h6 id="项目目录">项目目录</h6>
<pre><code class="language-text">├── &quot;BUILD&quot;
├── &quot;Makefile&quot;
├── &quot;WORKSPACE&quot;
├── src
│   ├── envoy                               -- envoy filter 插件源码
│   │   ├── alts
│   │   │   ├── *.cc
│   │   │   ├── *.h
│   │   │   └── &quot;BUILD&quot;
│   │   ├── &quot;BUILD&quot;
│   │   ├── http
│   │   │   ├── authn                     --认证 filte
│   │   │   │   ├── *.cc
│   │   │   │   ├── *.h
│   │   │   │   └── &quot;BUILD&quot;
│   │   │   ├── jwt_auth                    --jwt 认证 filter
│   │   │   │   ├── *.cc
│   │   │   │   ├── *.h
│   │   │   │   └── &quot;BUILD&quot;
│   │   │   └── mixer                      --mixer filter，实现metrics上报，Quota(Rate Limiting (处理http协议) 
│   │   │       ├── *.cc
│   │   │       ├── *.h
│   │   │       └── &quot;BUILD&quot;
│   │   ├── tcp
│   │   │   └── mixer                      --mixer filter(处理tcp协议)
│   │   │       ├── *.cc
│   │   │       ├── *.h
│   │   │       └── &quot;BUILD&quot;
│   │   └── utils
│   │       ├── *.cc
│   │       ├── *.h
│   │       └── &quot;BUILD&quot;
│   └── istio
│       └── **
├── test
│   └── **
└── tools
    └── **
</code></pre>
<p>makefile文件的关键信息:其中//src/envoy:envoy为bazel的语法，我们根据路径查询对应的内容；</p>
<pre><code class="language-bazel">build_envoy:
   export PATH=$(PATH) CC=$(CC) CXX=$(CXX) &amp;&amp; bazel $(BAZEL_STARTUP_ARGS) build $(BAZEL_BUILD_ARGS) $(BAZEL_CONFIG_REL) //src/envoy:envoy
</code></pre>
<p>/src/envops/BUILD文件关键内容为：cc_binary表明该target对应的是c++二进制文件路径，其中deps部分是其依赖的其他target。前13个target都是本地依赖，对应到源码目录中的其他子目录下的BUILD文件，其中最后一个比较特殊，是一个外部依赖，该外部库为envoy，因此proxy会make时音容envoy的源码，也算是扩展吧。</p>
<pre><code>envoy_cc_binary(
    name = &quot;envoy&quot;,
    repository = &quot;@envoy&quot;,
    visibility = [&quot;//visibility:public&quot;],
    deps = [
        // 对应本地文件
        &quot;//extensions/access_log_policy:access_log_policy_lib&quot;,
        &quot;//extensions/metadata_exchange:metadata_exchange_lib&quot;,
        &quot;//extensions/stackdriver:stackdriver_plugin&quot;,
        &quot;//extensions/stats:stats_plugin&quot;,
        &quot;//src/envoy/http/alpn:config_lib&quot;,
        &quot;//src/envoy/http/authn:filter_lib&quot;,
        &quot;//src/envoy/http/jwt_auth:http_filter_factory&quot;,
        &quot;//src/envoy/http/mixer:filter_lib&quot;,
        &quot;//src/envoy/tcp/forward_downstream_sni:config_lib&quot;,
        &quot;//src/envoy/tcp/metadata_exchange:config_lib&quot;,
        &quot;//src/envoy/tcp/mixer:filter_lib&quot;,
        &quot;//src/envoy/tcp/sni_verifier:config_lib&quot;,
        &quot;//src/envoy/tcp/tcp_cluster_rewrite:config_lib&quot;,
        // 对应外部的envoy
        &quot;@envoy//source/exe:envoy_main_entry_lib&quot;,
    ],
)
</code></pre>
<p>外部库定义在根目录下的workspace中，envoy的相关内容如图，在执行过程中，根据URL下载编译指定资源。</p>
<pre><code>http_archive(
    name = &quot;envoy&quot;,
    sha256 = ENVOY_SHA256,
    strip_prefix = &quot;envoy-wasm-&quot; + ENVOY_SHA,
    url = &quot;https://github.com/envoyproxy/envoy-wasm/archive/&quot; + ENVOY_SHA + &quot;.tar.gz&quot;,
)
</code></pre>
<p>编译过程依赖树如下<br>
<img src="https://img-blog.csdnimg.cn/20200225142650304.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2FreWoxMDIx,size_16,color_FFFFFF,t_70" alt="" loading="lazy"><br>
从istio-proxy项目中的Envoy BUILD中可以知道，这里会编译出name = &quot;Envoy&quot;的二进制程序，然后start_Envoy会启动Envoy，同时会根据一些默认参数和配置文件模板生成一个全新的配置文件，然后运行。一些关键参数如Envoy二进制的路径和配置路径、监听的端口、mixer的地址如下：<br>
<img src="https://img-blog.csdnimg.cn/20200225142725841.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2FreWoxMDIx,size_16,color_FFFFFF,t_70" alt="" loading="lazy"></p>
<h3 id="envoy">Envoy</h3>
<p>istio-proxy源码中提供了envoy.conf.template 通用配置模板，这个模板文件最终会生成一个envoy的配置文件，然后envoy启动的时候指定运行。模板配置文件中已经配置好了Mixer相关的参数如mixer_server，这个Mixer对于Envoy来说就是一个cluster，因此是在cluster_manager里面进行管理配置。</p>
<p>然后Envoy的官方文档Listener discovery service (LDS)一文中有说明静态的Listener文件配置是无法通过LDS API进行修改或删除的，因此静态配置会一直生效，istio-proxy源码中则提供了Envoy的静态配置文件envoy_lds.conf 静态listeners配置</p>
<h3 id="filter">Filter</h3>
<p><img src="https://img-blog.csdnimg.cn/20200225142902581.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2FreWoxMDIx,size_16,color_FFFFFF,t_70" alt="" loading="lazy"><br>
官方文档中描述的三个filtuers相关概念；Istio-proxy实现了Network::ReadFilter 和 Network::WriteFilter 过滤器，这样就可以通过filter API 绑定到Listener，然后当有数据读or写的时候调用到对应的filter了，这样数据流就从Envoy本身转到了filter中。</p>
<pre><code class="language-c++">// path:/src/envoy/tcp/mixer/filter.h
// Network::ReadFilter
Network::FilterStatus onData(Buffer::Instance &amp;data, bool) override;
 
// Network::WriteFilter
Network::FilterStatus onWrite(Buffer::Instance &amp;data, bool) override;
</code></pre>
<p>然后network filter再转到Envoy 的 http filter，根据官方文档HTTP filters的介绍也有两种Filters，为Decoder和Encoder，然后在istio-proxy源码中实现了 Http::StreamDecoderFilter和Http::StreamEncoderFilter这两个Filter，这样的话整个流程就串起来了。</p>
<pre><code class="language-c++">// path:/src/envoy/
//Http::StreamDecoderFilter
FilterHeadersStatus decodeHeaders(HeaderMap&amp; headers, bool) override;
FilterDataStatus decodeData(Buffer::Instance&amp; data, bool end_stream) override;
 
// Http::StreamEncoderFilter
FilterHeadersStatus encode100ContinueHeaders(HeaderMap&amp;) override {
  return FilterHeadersStatus::Continue;
}
FilterHeadersStatus encodeHeaders(HeaderMap&amp; headers, bool) override;
FilterDataStatus encodeData(Buffer::Instance&amp;, bool) override {
  return FilterDataStatus::Continue;
}
</code></pre>
<h3 id="mixer-client">Mixer client</h3>
<p>原有mixer client仓库是独立的，现在已经整合到了istio-proxy的代码仓库中，这样就可以很方便的在Sidecar Envoy代理中实现：</p>
<ul>
<li>和mixer server交互</li>
<li>添加一级缓存</li>
<li>前置检查</li>
<li>后置批量上报</li>
<li>策略控制</li>
<li>属性、字段转换和传递</li>
</ul>
<p>对此Istio-proxy的理解，这个Envoy的扩展，就是在Envoy基础上，增加了一些Filter，然后通过这些个Filter在利用Mixer Client和 Mixer Server进行通信，这样就可以在proxy代理中：</p>
<ul>
<li>流量代理</li>
<li>策略控制</li>
<li>遥测代理</li>
</ul>
<h3 id="envoy详解">Envoy详解</h3>
<p>Envoy是一个高性能的C++写的proxy转发器，那Envoy如何转发请求呢？需要定一些规则，然后按照这些规则进行转发。</p>
<p>规则可以是静态的，放在配置文件中的，启动的时候加载，要想重新加载，一般需要重新启动，但是Envoy支持热加载和热重启，一定程度上缓解了这个问题。</p>
<p>当然最好的方式是规则设置为动态的，放在统一的地方维护，这个统一的地方在Envoy眼中看来称为Discovery Service，过一段时间去这里拿一下配置，就修改了转发策略。</p>
<p>无论是静态的，还是动态的，在配置里面往往会配置四个东西:</p>
<ul>
<li>listener:也即envoy既然是proxy，专门做转发，就得监听一个端口，接入请求，然后才能够根据策略转发，这个监听的端口称为listener</li>
<li>endpoint:是目标的ip地址和端口，这个是proxy最终将请求转发到的地方。</li>
<li>cluster:一个cluster是具有完全相同行为的多个endpoint，也即如果有三个容器在运行，就会有三个IP和端口，但是部署的是完全相同的三个服务，他们组成一个Cluster，从cluster到endpoint的过程称为负载均衡，可以轮询等。</li>
<li>route:有时候多个cluster具有类似的功能，但是是不同的版本号，可以通过route规则，选择将请求路由到某一个版本号，也即某一个cluster。<br>
静态配置表<br>
<img src="https://img-blog.csdnimg.cn/20200225143131273.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2FreWoxMDIx,size_16,color_FFFFFF,t_70" alt="" loading="lazy"></li>
</ul>
<h3 id="envoy启动过程解析">Envoy启动过程解析</h3>
<p>Envoy源码中启动入口函数在source/exe/main.cc</p>
<pre><code class="language-c++">std::unique_ptr&lt;Envoy::MainCommon&gt; main_common;
// 声明并初始化Envoy::MainCommon实例为main_common
try {
  main_common = std::make_unique&lt;Envoy::MainCommon&gt;(argc, argv);
} catch (const Envoy::NoServingException&amp; e) {
  return EXIT_SUCCESS;
} catch (const Envoy::MalformedArgvException&amp; e) {
  std::cerr &lt;&lt; e.what() &lt;&lt; std::endl;
  return EXIT_FAILURE;
} catch (const Envoy::EnvoyException&amp; e) {
  std::cerr &lt;&lt; e.what() &lt;&lt; std::endl;
  return EXIT_FAILURE;
}
// 声明并初始化Envoy::MainCommon实例为main_common，执行main_common-&gt;run启动Server
return main_common-&gt;run() ? EXIT_SUCCESS : EXIT_FAILURE;
</code></pre>
<p>MainCommon关键代码</p>
<pre><code class="language-c++">class MainCommon {
public:
  MainCommon(int argc, const char* const* argv);
  // 启动函数
  bool run() { return base_.run(); }
  .............
  Server::Instance* server() { return base_.server(); }
  MainCommonBase base_;
};
</code></pre>
<p>MainCommonBase.cc关键代码</p>
<pre><code class="language-c++">// main_common.cc
int main_common(OptionsImpl&amp; options) {
  try {
      // 生成maincommonbase，在里面会做server instance的初始化
    MainCommonBase main_common(options);
    return main_common.run() ? EXIT_SUCCESS : EXIT_FAILURE;
  } catch (EnvoyException&amp; e) {
    return EXIT_FAILURE;
  }
  return EXIT_SUCCESS;
}
 
MainCommonBase::MainCommonBase(OptionsImpl&amp; options) : options_(options) {
    ......
    // 可以看到，MainCommon将会初始化Instance，即一个服务的实例，于是，InstanceImpl进行初始化
    server_.reset(new Server::InstanceImpl(
        options_, local_address, default_test_hooks_, *restarter_, *stats_store_, access_log_lock,
        component_factory_, std::make_unique&lt;Runtime::RandomGeneratorImpl&gt;(), *tls_));
    ......
}
</code></pre>
<p>Instance会启动初始化，在初始化核心函数中，将会进行listenerConfig的全面注册</p>
<pre><code class="language-c++">// server.cc
InstanceImpl::InstanceImpl(Options&amp; options, Network::Address::InstanceConstSharedPtr local_address,
                           TestHooks&amp; hooks, HotRestart&amp; restarter, Stats::StoreRoot&amp; store,
                           Thread::BasicLockable&amp; access_log_lock,
                           ComponentFactory&amp; component_factory,
                           Runtime::RandomGeneratorPtr&amp;&amp; random_generator,
                           ThreadLocal::Instance&amp; tls) {
    ......  
    initialize(options, local_address, component_factory);
    ......
}
 
void InstanceImpl::initialize(Options&amp; options,
                              Network::Address::InstanceConstSharedPtr local_address,
                              ComponentFactory&amp; component_factory) {
    ...
    // Handle configuration that needs to take place prior to the main configuration load.
    InstanceUtil::loadBootstrapConfig(bootstrap_, options,
                 messageValidationContext().staticValidationVisitor(), *api_);
    .......
    // 初始化ListenerManager
    listener_manager_.reset(new ListenerManagerImpl(
      *this, listener_component_factory_, worker_factory_, ProdSystemTimeSource::instance_));
 
    // 会初始化
    main_config-&gt;initialize(bootstrap_, *this, *cluster_manager_factory_);
 
    ...
}
// 通过loadFromFile和loadFromYaml读取配置文件路径下的配置，并完成参数校验。
InstanceUtil::loadBootstrapConfig(envoy::config::bootstrap::v2::Bootstrap&amp; bootstrap,
                                  Options&amp; options) {
  try {
      // 根据配置信息查找对应配置文件
    if (!options.configPath().empty()) {
      MessageUtil::loadFromFile(options.configPath(), bootstrap);
    }
    if (!options.configYaml().empty()) {
      envoy::config::bootstrap::v2::Bootstrap bootstrap_override;
      MessageUtil::loadFromYaml(options.configYaml(), bootstrap_override);
      bootstrap.MergeFrom(bootstrap_override);
    }
    MessageUtil::validate(bootstrap);
    return BootstrapVersion::V2;
  } catch (const EnvoyException&amp; e) {
    if (options.v2ConfigOnly()) {
      throw;
    }
    // TODO(htuch): When v1 is deprecated, make this a warning encouraging config upgrade.
    ENVOY_LOG(debug, &quot;Unable to initialize config as v2, will retry as v1: {}&quot;, e.what());
  }
  if (!options.configYaml().empty()) {
    throw EnvoyException(&quot;V1 config (detected) with --config-yaml is not supported&quot;);
  }
  Json::ObjectSharedPtr config_json = Json::Factory::loadFromFile(options.configPath());
  Config::BootstrapJson::translateBootstrap(*config_json, bootstrap);
  MessageUtil::validate(bootstrap);
  return BootstrapVersion::V1;
}
 
void MainImpl::initialize(const envoy::config::bootstrap::v2::Bootstrap&amp; bootstrap,
                          Instance&amp; server,
                          Upstream::ClusterManagerFactory&amp; cluster_manager_factory) {
  ......
  const auto&amp; listeners = bootstrap.static_resources().listeners();
  ENVOY_LOG(info, &quot;loading {} listener(s)&quot;, listeners.size());
  // 从bootstrap配置（yaml文件）中提取listener配置，并依次进行添加操作。
  for (ssize_t i = 0; i &lt; listeners.size(); i++) {
    ENVOY_LOG(debug, &quot;listener #{}:&quot;, i);
    server.listenerManager().addOrUpdateListener(listeners[i], &quot;&quot;, false);
  }
  ...... 
</code></pre>
<p>回到MainCommonBase.run方法</p>
<pre><code class="language-c++">bool MainCommonBase::run() {
switch (options_.mode()) {
case Server::Mode::Serve:
  server_-&gt;run();
  return true;
case Server::Mode::Validate: {
  auto local_address = Network::Utility::getLocalAddress(options_.localAddressIpVersion());
  return Server::validateConfig(options_, local_address, component_factory_, thread_factory_,
                                file_system_);
}
case Server::Mode::InitOnly:
  PERF_DUMP();
  return true;
}
NOT_REACHED_GCOVR_EXCL_LINE;
}
</code></pre>
<p>调用到InstanceImpl的run方法</p>
<pre><code class="language-c++">void InstanceImpl::run() {
  // startWorker即会进行eventloop
  RunHelper helper(*dispatcher_, clusterManager(), restarter_, access_log_manager_, init_manager_,
                   [this]() -&gt; void { startWorkers(); });
  ......
}
 
void InstanceImpl::startWorkers() {
  listener_manager_-&gt;startWorkers(*guard_dog_);
  ......
}
</code></pre>
<p>InstanceImpl::startWorkers()方法解析</p>
<pre><code class="language-c++">void ListenerManagerImpl::startWorkers(GuardDog&amp; guard_dog) {
  ENVOY_LOG(info, &quot;all dependencies initialized. starting workers&quot;);
  ASSERT(!workers_started_);
  workers_started_ = true;
  for (const auto&amp; worker : workers_) {
    ASSERT(warming_listeners_.empty());
    for (const auto&amp; listener : active_listeners_) {
      // 此处即会将所有listener绑定到所有worker身上。worker即服务的并发线程数。
      addListenerToWorker(*worker, *listener);
    }
    worker-&gt;start(guard_dog);
  }
}
 
void WorkerImpl::addListener(Network::ListenerConfig&amp; listener, AddListenerCompletion completion) {
   ......
   handler_-&gt;addListener(listener);
   ......
}
</code></pre>
<p>进一步看addListener()方法</p>
<pre><code class="language-c++">void ConnectionHandlerImpl::addListener(Network::ListenerConfig&amp; config) {
  // 生成ActiveListener
  ActiveListenerPtr l(new ActiveListener(*this, config));
  listeners_.emplace_back(config.socket().localAddress(), std::move(l));
}
 
ConnectionHandlerImpl::ActiveListener::ActiveListener(ConnectionHandlerImpl&amp; parent,
                                                      Network::ListenerConfig&amp; config)
    : ActiveListener(
          parent,
          // 可以看到，在ActiveListener初始化过程中，将进行真正Listener的初始化。
          parent.dispatcher_.createListener(config.socket(), *this, config.bindToPort(),
                                            config.handOffRestoredDestinationConnections()),
          config) {}
 
ListenerImpl::ListenerImpl(Event::DispatcherImpl&amp; dispatcher, Socket&amp; socket, ListenerCallbacks&amp; cb,
                           bool bind_to_port, bool hand_off_restored_destination_connections)
    : local_address_(nullptr), cb_(cb),
      hand_off_restored_destination_connections_(hand_off_restored_destination_connections),
    ......
    // 通过libevent的`evconnlistener_new`实现对指定监听fd的新连接事件的回调处理。
    listener_.reset(
        evconnlistener_new(&amp;dispatcher.base(), listenCallback, this, 0, -1, socket.fd()));
    ......
}
Listener对新连接设置回调函数 &amp; Listener Filter创建
void ListenerImpl::listenCallback(evconnlistener*, evutil_socket_t fd, sockaddr* remote_addr,
                                  int remote_addr_len, void* arg) {
  ......
  // 此处的fd已经不是listenfd，已经是该新连接的connfd。
  listener-&gt;cb_.onAccept(std::make_unique&lt;AcceptedSocketImpl&gt;(fd, local_address, remote_address),
                         listener-&gt;hand_off_restored_destination_connections_);
  ......
}
 
// 回调时候主要做两件事情，
// 1. 构建出对应的Listener Accept Filter
// 2. 构建出ServerConnection
void ConnectionHandlerImpl::ActiveListener::onAccept(
    Network::ConnectionSocketPtr&amp;&amp; socket, bool hand_off_restored_destination_connections) {
  ......
  auto active_socket = std::make_unique&lt;ActiveSocket&gt;(*this, std::move(socket),
                                                 hand_off_restored_destination_connections);
  // 构建对应的Filter
  config_.filterChainFactory().createListenerFilterChain(*active_socket);
  active_socket-&gt;continueFilterChain(true);
  ......
}
 
void ConnectionHandlerImpl::ActiveSocket::continueFilterChain(bool success) {
   ......
   // 创建连接
   listener_.newConnection(std::move(socket_));
   ......  
}
</code></pre>
<p>我们接着newConnecion方法看</p>
<pre><code class="language-c++">void ConnectionHandlerImpl::ActiveListener::newConnection(Network::ConnectionSocketPtr&amp;&amp; socket) {
  ......
  auto transport_socket = filter_chain-&gt;transportSocketFactory().createTransportSocket();
  // 创建ServerConnection
  Network::ConnectionPtr new_connection =
      parent_.dispatcher_.createServerConnection(std::move(socket), std::move(transport_socket));
  new_connection-&gt;setBufferLimits(config_.perConnectionBufferLimitBytes());
  // 创建真正的Read/Write Filter
 const bool empty_filter_chain = !config_.filterChainFactory().createNetworkFilterChain(
      *new_connection, filter_chain-&gt;networkFilterFactories());
  ......
}
 
ConnectionImpl::ConnectionImpl(Event::Dispatcher&amp; dispatcher, ConnectionSocketPtr&amp;&amp; socket,
                               TransportSocketPtr&amp;&amp; transport_socket, bool connected)
    : transport_socket_(std::move(transport_socket)), filter_manager_(*this, *this),
      socket_(std::move(socket)), write_buffer_(dispatcher.getWatermarkFactory().create(
                                      [this]() -&gt; void { this-&gt;onLowWatermark(); },
                                      [this]() -&gt; void { this-&gt;onHighWatermark(); })),
      dispatcher_(dispatcher), id_(next_global_id_++) {
    // 当read/writed生成事件
  file_event_ = dispatcher_.createFileEvent(
      fd(), [this](uint32_t events) -&gt; void { onFileEvent(events); }, Event::FileTriggerType::Edge,
      Event::FileReadyType::Read | Event::FileReadyType::Write);
}
</code></pre>
<p>初始化完了连接和Listener，也初始化完了Accept Listener Filter和Read/Write Listener Filter，此时，Listener的libevent事件已经准备就绪，有请求到来后，Connection的read/write事件也将被触发。此时Worker启动源码.</p>
<pre><code class="language-c++">void WorkerImpl::start(GuardDog&amp; guard_dog) {
  ASSERT(!thread_);
  thread_.reset(new Thread::Thread([this, &amp;guard_dog]() -&gt; void { threadRoutine(guard_dog); }));
}
 
void WorkerImpl::threadRoutine(GuardDog&amp; guard_dog) {
  ......
  dispatcher_-&gt;run(Event::Dispatcher::RunType::Block);
 
  // 异常退出后做的清理操作
  guard_dog.stopWatching(watchdog);
  handler_.reset();
  tls_.shutdownThread();
  watchdog.reset();
}
 
void DispatcherImpl::run(RunType type) {
  // 启动libevent处理
  event_base_loop(base_.get(), type == RunType::NonBlock ? EVLOOP_NONBLOCK : 0);
}
</code></pre>
]]></content>
    </entry>
</feed>